<div class="row">
    <div class="col-8 my-4">
  <h2>このサイトに投稿されている掲示板は１時間で消えます。だからこそ、過去の古い投稿などは無く、今ゲームをプレイしている人しかいません！</h2>
</div>
<div class="col-4 p-2 my-4 bg-info">
<h2>ログイン</h2>
<%= form_with model: @user, url: user_session_path do |f| %>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "text_field" %>
  </div>

  <div class="field">
    <%= f.label :password %><%= link_to "パスワードを忘れた方", new_password_path(resource_name) %><br />
    <%= f.password_field :password, autocomplete: "current-password", class: "text_field" %>
  </div>

  <% if devise_mapping.rememberable? %>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>
  <% end %>

  <div class="mt-2 actions">
    <%= f.submit "ログイン", class: "btn btn-outline-success" %>
  </div>
<% end %>

<div class="mt-2">
<%= link_to "新規登録", new_registration_path(resource_name), class: "btn btn-outline-success"  %>
</div>
</div>
</div>
</div>
